# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is currently empty. It is intended to become an expense tracker application, but no code, configuration, or documentation has been added yet.

## Next steps for future sessions

Once code is added to this repository, update this file with:

- **Commands**: how to install dependencies, build, lint, test, and run the app (and how to run a single test).
- **Architecture**: the overall structure of the app (e.g., frontend/backend split, data storage, key modules) and how the major pieces fit together.
- **Conventions**: any project-specific rules pulled from a README, `.cursorrules`, `.cursor/rules/`, or `.github/copilot-instructions.md` if those are added later.

Re-run `/init` after the initial project scaffold is in place to regenerate this file with real details.
